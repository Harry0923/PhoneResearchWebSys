import { ExcelPasteDirective } from './excel-paste.directive';

describe('ExcelPasteDirective', () => {
  it('should create an instance', () => {
    const directive = new ExcelPasteDirective();
    expect(directive).toBeTruthy();
  });
});
